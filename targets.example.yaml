- uri: https://reddit.com
  interval: 6s
  regexp: ".*redux_json_start.*"
  selector:
    target: reddit
    enable: true

- uri: https://linux.org.ru
  interval: 5s
  regexp: ".*ubuntu.*"
  selector:
    target: lor
    enable: false
